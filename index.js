const divide = function() {
   let num1= 2000
   let num2=100
let result=num1/num2 
    return result;
  }
  divide();

  const square = x => x * x;
  square(3);

  const add = (x,y) => x + y;
  add(4,5)